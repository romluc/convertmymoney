<%- include('header')%>
<div class="result">
  <% if (error) { %>
  <%= error %>
  <% } else { %>
  <p>You have: $ <%= quantity %></p>
  <p>Today's Quotation: BRL <%= quotation%></p>
  <p>Your converted total in BRL is: R$ <%= conversion %></p>
  <% } %>
</div>
<%- include('footer')%>